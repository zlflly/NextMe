单列主键表的连接

多列混合主键表的连接



条件连接：

s是A表的一个属性，t B

把符合s $\theta$ t的数据条目连接起来（A在前，B在后）

​	可以看成，拿A的每一条数据，遍历B中符合条件的数据

​	也可以看成是一个笛卡尔积+选择

自然连接：（表合并）

​	按照属性相同的列（有几个用几个，不用显示声明用哪几列）拼接，去掉重复的列，和不匹配的数据条目

等值连接：条件连接的特殊情况

自然连接和等值连接什么时候是一致的

​	是不是选出一个数据的某一个元素，拿着这个元素去大表当中找元组时，二者是一致的



左外连接，左侧表在右侧不存在的填null（左右都不损失数据，只是左侧中空位填null）



除运算：（除运算的导出表达式）





选择表达式当中的“不选”：全部的 - 选了的

选择一个属性当中的所有值

​	$\Pi_{Cno}(C)$

选择与李勇同一个系的同学的信息

​	选出李永所在的系当中的所有人（投影）

​	然后用除法，就选出了全部和李永同xi





插入 并上一条新信息

删除

​	删除 刘明亮 选修的 英语课

​	

考试对关系演算不做要求